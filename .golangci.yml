run:
  timeout: 10m

linters:
  enable:
    - structcheck
    - goimports
    - gosimple
    - staticcheck
    - gosec
  disable:
    - errcheck

linters-settings:
  gosec:
    excludes:
      - G108

issues:
  exclude-rules:
    - path: tests
      linters:
        - gosec