apiVersion: core.pingcap.com/v1alpha1
kind: SchedulerGroup
metadata:
  name: scheduler
spec:
  cluster:
    name: pdms
  replicas: 1
  template:
    metadata:
      annotations:
        author: pingcap
    spec:
      version: v8.1.0
      config: |
        [log]
        level = "debug"
