apiVersion: cert-manager.io/v1alpha2
kind: Certificate
metadata:
  name: monitor-cluster-client-secret
  namespace: default
spec:
  secretName: monitor-cluster-client-secret
  duration: 8760h # 365d
  renewBefore: 360h # 15d
  organization:
    - PingCAP
  commonName: "TiDB"
  usages:
    - client auth
  issuerRef:
    name: tidb-issuer
    kind: ClusterIssuer
    group: cert-manager.io
